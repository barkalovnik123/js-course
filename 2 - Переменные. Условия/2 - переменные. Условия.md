# Переменные. Условия.

## Переменные

Переменная - контейнер, в который можно положить значения. Бывают двух видов

```
let a; // обычная переменная
const a; // Константа - неизменяемая переменная
```

## Условия

```
if () {} else if () {} else {}
```
